# PhotoGen App - GPU Requirements (Local + API Support)
# For users with NVIDIA GPUs who want to run models locally AND use API services

# --- Core ML & Diffusers for GPU ---
# CUDA 12.1 and higher support
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
diffusers @ git+https://github.com/huggingface/diffusers.git@425a715e35479338c06b2a68eb3a95790c1db3c5
transformers>=4.35.0
accelerate>=0.24.0

# --- Custom Model Backend for Local Processing ---
nunchaku @ git+https://github.com/mit-han-lab/nunchaku.git

# --- Web UI Framework ---
gradio==5.41.1
gradio-client==1.11.0
fastapi==0.116.1
uvicorn[standard]==0.29.0

# --- API Clients & Web Services ---
requests>=2.31.0
python-dotenv>=1.0.0
openai>=1.0.0  # For OpenAI-compatible APIs (Qwen, etc.)

# --- Core Utilities ---
PyYAML>=6.0.0
cryptography>=41.0.0
Pillow>=10.0.0
numpy>=1.24.0

# --- Optional GPU Optimization ---
xformers  # Memory-efficient attention (optional but recommended)
